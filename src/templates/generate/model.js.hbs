var {{objectName}} = DS.Model.extend(Ember.Validations.Mixin,{
{{#each fields}}
  {{name}}: DS.attr('{{type}}'){{comma}}
{{/each}}
  ,
  validations: {
    /*
    message: {
      presence: true,
      length: { minimum: 40 }
    },
    */
  }
});

module.exports = {{objectName}};

