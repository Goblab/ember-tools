<section class="container">
	<div class="page-header">
    <h3>{{title}}</h3>
	</div>
	<div class="">
	\{{#form-for controller}}	
		{{#each fields}}
			\{{input {{name}} }}
		{{/each}}		
		\{{submit}}
	\{{/form-for}}
	</div>
</section>